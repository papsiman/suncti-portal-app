CREATE TABLE users (
    Id int NOT NULL AUTO_INCREMENT,
    Username varchar(255) NOT NULL,
    Password varchar(255),    
    Role varchar(255),
    PRIMARY KEY (Id)
);

CREATE TABLE config (
    name varchar(255) NOT NULL,
    value varchar(255)
);

insert into config values ('config','')